
syntax = "proto3";

package programus.proto;


// TODO(bgrzesik): delete me
message TestMessage {
  string value = 1;
}

message GenericMessage {
  uint64 sessionId = 1;

  // Ordinal
  uint64 requestId = 2;

  oneof payload {
    TestMessage test = 100;
  }
}